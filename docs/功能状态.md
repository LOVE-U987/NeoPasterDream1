# 功能状态

> **类别**：活文档 · 玩家可见功能与还原覆盖  
> **日期**：2026-08-02  
> **命令 / 套件** → [`验证复现.md`](验证复现.md)  
> **注释过时** → [`注释审计.md`](注释审计.md)  
> **实现策略** → [`架构差异.md`](架构差异.md)

| | |
| :--- | :--- |
| **原版** | `libs/FixPasterDream-main/` · Forge 1.20.1 · MCreator |
| **重写版** | `PasterDream/` + `PasterDreamAPI/` · NeoForge 1.21.1 |
| **图例** | 🟢 已还原 · 🔵 重构增强 · 🟡 部分 · ⚪ 有意排除 · 🔴 仍缺 |
| **锚点** | `5f465da` 主路径全量 + 后续 curios/工坊/展台/交易等收口 |

---

## 1. 终验总览

### 1.1 注册与行为

| # | 维度 | 期望 | 在场 | 覆盖 | 备注 |
|---|------|-----:|-----:|:----:|------|
| 1 | 方块 | 334 | 334 | **100%** | 另有增强：eoul_doll、love_u_doll 等 |
| 2 | 物品 | 690 | 690 | **100%** | 运行时另有调试杖/刷怪蛋等 extra |
| 3 | 实体 | 51 | 51 | **100%** | 含法术/法杖投射物与立场 |
| 4a | 状态效果 | 46 | 46 | **100%** | cook/oppression/fury 等已挂 PDAttributes |
| 4b | 药水 | 4 | 4 | **100%** | |
| 5 | 附魔 | 2 | 2 | **100%** | 数据驱动 `enchantment/` |
| 6 | 粒子 | 34 | 34 | **100%** | |
| 7 | 音效 | 68 | 68 | **100%** | ≥ 原版键；另有 BGM 扩展 |
| 8 | 维度 | 4 | 4 | **100%** | dyedream / lamp_shadow / wind_journey / aaroncos_arena |
| 9 | 菜单 | 19 | 19 | **100%** | 工坊三 GUI、高炉、研究台、笔记/蓝图、容器等 |
| 10 | 方块实体 | 79 | 79 | **100%** | |
| 11 | 属性 | 10 | 10 | **100%** | skill / teleportation / san / magic / healing 等 |
| 12 | 创造标签 | — | — | n/a | 原版 tab 语义已重构，不按旧名 diff |
| 13 | 成就 | 62 | 62 | **100%** | JSON 全量；代码授予 hide_5/6/treasure_start 等已接线；中英 title/descr |
| 14 | 配方 | — | 444 | — | 含 shadow_blasting×5；`crafting_doremys_guidebook` 仅当 Patchouli 在场加载 |
| 15 | 行为断言 | — | **250+** | **100%** | structures×20 · workshop×28 · struct-dim×15 · gallery×3 · entity-gallery×5 · **curios×53** · stale-comments 等 |

静态门禁：`check_lang` 全绿 · `verify_resource_closure` PASS。  
CORE 近次：**140/0**（含 curios×53）。

**不适用 / 新版扩展**：`processor_list`、`damage_type`（两边 0）；`noise_settings`（新版 +1）；`jukebox_song`（新版 +10）。

### 1.2 统计口径

| 项 | 说明 |
| :--- | :--- |
| 注册期望 | `pd_porting_manifest.json` → `PDPortingVerifyTest` 运行时注册表 diff |
| 行为 | VERIFY 时间线：效果、维度、法术、高炉、法杖、笔记/蓝图/袋、工坊、结构、展台、饰品 |
| 静态 | `.trae/tools/check_lang.py` · `verify_resource_closure.py` |
| 构建 | Java 21 · `sh gradlew :PasterDream:compileJava --offline` |

---

## 2. 模块还原状态

### 2.1 世界与维度

| 模块 | 状态 | 证据 |
|------|:----:|------|
| 染梦维度生成 | 🔵 | 自定义 biome source / chunkgen；深海/浅海/**海岸**/平原/森林/高地/**密林**/雪原/蘑菇等；狐狸雕像仪式 + VERIFY `dyedream` |
| 染梦遗迹群 | 🟢 | RuinAPI / PDRuinsRegistration×41；教堂 0–10 全量 |
| 影灯世界 | 🟢 | `lamp_shadow_world`；struct-dim |
| 风之旅途 | 🟢 | `wind_journey_world`；SanHelper 云雾出维；ores/草花；Boss loot/AOE；雷云落雷；风向 Pr0–2（离维清顺/逆风残留）；破风幕 **FACING**+弹射；云块 friction/speed/jump；纸飞机 Curio amp；迷梦进维 **Y clamp**；水色湖常驻；VERIFY `wind-journey`；流程 [`第三梦境.md`](archive/游戏流程分析/第三梦境.md)；P0.5 遗迹 locate 已过，祭坛手感见 §3 |
| 亚伦柯斯竞技场 Boss | 🟢 | `PDArenaBossManager`、左右手、Boss 条 |
| 结构触发方块 0–23 | 🟢 | `PDStructureBlock` + NBT；放置后自毁（设计如此） |
| 音乐 / BGM | 🔵 | 渐变系统、`/pasterdream bgm`、jukebox_song×10 |

### 2.2 工作站与容器

| 模块 | 状态 | 证据 |
|------|:----:|------|
| 武器工坊 | 🟢 | 铁砧/锻炉/冷却/磨盘 + 三 GUI；workshop×28 |
| 暗影高炉 | 🟢 | BE + 5 配方 + JEI |
| 梦境炼药锅 | 🟢 | Menu/Screen + SMOKETEST；JEI |
| 蓄梦池 / 融梦箱 | 🟢 | BE + GUI |
| 收纳袋 / 野餐篮 / 风泊木箱 | 🟢 | `PDMenus` / `PDMenusFurniture` |
| 蓝图 / 研究台 | 🟢 | BlueprintGui0 + ResearchTable |

### 2.3 战斗与能力

| 模块 | 状态 | 证据 |
|------|:----:|------|
| 法术 / 魔杖 | 🟢 | 五法术 + 投射物族 + 立场 |
| 唤星裂隙 / 照明 | 🟢 | 限时**方块**（非实体）：裂隙下射、照明粒子 |
| 瞬身术 | 🟢 | Payload + 键位 + 属性 CD/消耗/距离 |
| 斗篷 | 🟢 | `CloakActivatePayload`、键位 Z、Curios back |
| 自定义属性 | 🟢 | `PDAttributes`×10 |
| San 理智 | 🟢 | `SanData` + overlay + gamerule |
| 融梦能量 | 🟢 | `MeltDreamEnergyData` + HUD + 能量戒指 |
| 生物 AI / 生态 | 🟢 | GeckoLib + AI；loot 闭包 |

### 2.4 进度、收集与 UI

| 模块 | 状态 | 证据 |
|------|:----:|------|
| 成就 | 🟢 | 62/62 · 中英 |
| 梦境笔记 | 🟢 | notes 0–14 + GUI |
| 卡勒卡牌 | 🟢 | 0–9 效果 + 全屏 displayItemActivation + 卡 6 熔金坠落 |
| 天赋抉择（光/影） | 🟢 | `ShadowSelectEndMenu` |
| 纪念物 | 🟢 | memento 01–11 + buff |
| HUD | 🟢 | Boss / San / 能量 / 生命 / Cloudmist |
| 内置 UI 包 | 🟢 | `packs/paster_vanilla_ui` |

### 2.5 经济、数据与联动

| 模块 | 状态 | 证据 |
|------|:----:|------|
| 梦币 + 村民交易 | 🟢 | 流浪商人×5 + 工具匠蓝图 + 农夫酵母 |
| soul_dust 熔炉燃料 | 🟢 | data_maps furnace_fuels → 800 |
| 自定义画 ×2 | 🟢 | painting_variant + 贴图 + 译键 |
| Curios 饰品 | 🔵 | players 槽含 ring×2、curio/hands/bracelet；纸飞机 force NBT；**VERIFY 已按描述/原版数值全等重写**（`PDCurioVerifyHooks`：attr/unique/tick/desc/equip）。实现缺口见 §3.2 |
| 游戏规则 / 配置 | 🟢 | `PDGameRules`；Client/Common Config + **原生 `PDConfigScreen`**（约 47 项 / 7 类）；San/能量总开关、HUD、`LOW_SAN_PICTURE_JITTER`、`BAN_TIME_HOURGLASS`、赠针默认等已接线 |
| 通用标签 | 🟢 | 大量 `c:` 矿/食/木/装备/存储块等 + mineable 等原版 tag 修补 |
| JEI | 🟢 | 炼药锅 / 陶锅 / 高炉 / 工坊（可选依赖；未装不影响主模） |
| Patchouli 图鉴 | 🟢 | **可选 · 纯数据手册包**（`data/.../patchouli_books` + `assets/.../patchouli_books`）。书 `doremys_guidebook`：分类 overview/rules、词条×6（四维+SAN+融梦）、i18n 中英、模型贴图；`use_resource_pack=true`。**1.21 配方兼容**：旧 `patchouli:shapeless_book_recipe` 已移除 → `minecraft:crafting_shapeless` 产出 `patchouli:guide_book` + 组件 `patchouli:book=pasterdream:doremys_guidebook`；外层 `neoforge:mod_loaded=patchouli`。不声明 `custom_book_item`（主模无独立书物品）。**无 Java 硬依赖**；`mods.toml` optional；开发 `localRuntime` 1.21.1-93-NEOFORGE。未装 Patchouli 时主模正常、配方跳过。VERIFY `newdata`：在场 ⇔ 配方已加载 |
| 命令 | 🟡 | `/pasterdream dimension reset` · `bgm`；原版三条未原样 |
| 玩家动画姿势 | 🟢 | playeranimator + evasion/none 资源；闪避 S2C 驱动 |
| API 上收 | 🔵 | P0/P1/**P2** 已入 `PasterDreamAPI` → [`架构差异.md`](架构差异.md) §3.7–3.8；**勿上收** → [`主模可上收API候选.md`](主模API上收.md) |
| Tetra / Croptopia | ⚪ | Tetra 无 1.21.1；Croptopia 深度未做 |

---

## 3. 后续与开放项

主路径注册与自动化行为已闭环。**只维护下表；勿把已关闭项写回。**

### 3.1 建议优先级

| 优先级 | 方向 |
| :---: | :--- |
| 🔥 高 | **饰品描述对齐**：按严格 VERIFY 修属性/tick/注册缺口（见 §3.2 饰品清单），再跑 `SUITES=core` 至 curios 0 fail |
| 🔥 高 | **人工游玩回归**：暗影成就链、真影之床→影之抉择、灯影返程落点、工坊手感、法杖耗能/CD、Curios 佩戴、唤星裂隙下射 |
| 🔶 中 | **风之旅途祭坛 mesh 手测收口**（BE/Codec 已修 + VERIFY BE type；仍须并排/交互观感） |
| 🌙 低 | 暮影 rings 手感调参；原版三条调试命令原样；第三方 Croptopia；Tetra 待上游 |
| 📄 文档 / 卫生 | 中英抽检；BGM 边界手感 |

### 3.2 仍开放明细（真待办）

| 项 | 说明 | 优先级 |
|----|------|:------:|
| **饰品严格 VERIFY → 实现对齐** | `PDCurioVerifyHooks` 已按**描述 + 原版数值全等**重写（不再“升高即可”）。跑 `PASTERDREAM_VERIFY_SUITES=core` 后按 FAIL 修实现；旧口径 curios×53/CORE 140/0 **作废**，以新报告为准。细则见下表 | 高 |
| 风旅祭坛 0→4 mesh 手测 | Codec/BE type/交互服务端权威已修；VERIFY 断言 BE type。**仍须**并排 setblock + 交互推进观感（贴图原版共用，差在 geo） | 中 |
| 暮影 rings 手感 | `shadow_world_doors` concentric_rings 散布可调；非功能缺口 | 低 |
| 原版三条命令原样 | san/structure/tp_world 调试；现由 `/pasterdream dimension|bgm` 等部分替代 | 低 |
| Tetra / Croptopia | 上游无 1.21.1 / 深度未做 | — |

#### 3.2.1 饰品 VERIFY / 实现对齐清单（测试 todo）

**跑法**

```bash
PASTERDREAM_VERIFY=1 PASTERDREAM_VERIFY_SUITES=core PASTERDREAM_VERIFY_KEEP_OPEN=0 \
  JAVA_HOME=/usr/lib/jvm/java-21-openjdk \
  sh gradlew :PasterDream:runClient --offline
# 看日志 [curios] 与 run/pd_verify_report.json
```

**断言规则（已写入 hooks，修实现时勿放宽）**

- 属性：`amount` + `Operation` **全等**；不得用 Δ>0 / 近似
- tick 效果：amp + duration **全等**（如夜视 amp0/d240、塞西莉亚抗性 amp4/d100）
- 能量：融梦环 **+0.0025/20t（=0.15/min）**；种荚 **+0.1 能量 +0.4 exhaustion /20t**
- 唯一：`canEquip` 第一枚 true、已装备后再 false
- 描述：tooltip 须含描述中的关键数字/关键词（`desc-*`）
- 缺失注册或空实现 → **FAIL**（不 skip）

| # | 用例 / 物品 | 断言要点（描述/原版） | 现状（预判） | 状态 |
|--:|---|---|---|:---:|
| 1 | `attr-fourleaf_clover_curio` | max_health **+1** ADD；luck **+6** ADD | 注册走 CurioAPI.attribute；另有空壳 `FourleafCloverCurioItem` 勿双轨 | ✅ |
| 2 | `attr-hithard_0_ring` | attack **+0.5** ADD | 已补 getAttributeModifiers +0.5 ADD | ✅ |
| 3 | `attr-hithard_1_ring` | attack **+1** ADD | 同上，补 +1 ADD | ✅ |
| 4 | `attr-red_dew_0..3_ring` | max_health **+1/+2/+3/+4** ADD | 已从 +0.5 修正为 +1/+2/+3/+4 | ✅ |
| 5 | `attr-health_0_necklace` | max_health **+2** ADD | 已补 getAttributeModifiers | ✅ |
| 6 | `attr-carapax_charm` | speed **\*−0.08** MULT_BASE；armor **+2**；toughness **+1** | 同上 | ✅ |
| 7 | `attr-allkinds_ring` | hp+4 / atk+2 / atkspd+0.1 / entity_reach+0.2 / block_reach+0.5 / speed\*+0.05 / skillcd−0.05 / skillmult+0.05 / tpcd−0.05 | 同上，9 属性齐全 | ✅ |
| 8 | `attr-boboji_plume` | tp_consume **−0.2 与 −0.4**；tp_range **+0.1**；speed **\*+0.05**（对齐原版 map） | 同上 | ✅ |
| 9 | `attr-feather_necklace` | tp_consume **−0.05**；tp_range **+0.2** | 同上 | ✅ |
| 10 | `attr-dream_traveler_belt` | speed **\*+0.03**；tpcd **−0.1** | 同上 | ✅ |
| 11 | `attr-traveler_belt` | tp_consume **−0.5** | 同上 | ✅ |
| 12 | `attr-dark_alllegory_curio` | magiccd **−0.1**；magicpower **+1** | 同上 | ✅ |
| 13 | `attr-degenerate_bodys` | hp **−4**；entity_reach+0.2；block_reach+1；atk+2 | 同上 | ✅ |
| 14 | `attr-duke_coin_curio` | luck **+7**；tpcd **+7**（同 modifier 绑两属性，原版如此） | 同上 | ✅ |
| 15 | `attr-garland` / `attr-nature_belt` | san_variability **+0.48** | 已补 getAttributeModifiers | ✅ |
| 16 | `attr-sea_charm` | swim_speed **+0.3**；san_var **+0.96** | 同上（swim_speed 用 NeoForgeMod） | ✅ |
| 17 | `attr-hiyori_head` | hp **+2**；luck **+15**；san_var **+0.96** | 同上 | ✅ |
| 18 | `unique-*`（上表唯一件） | canEquip 第一 true / 已装 false | 全部已通过 CurioHelper.canEquipSingleton 实现 | ✅ |
| 19 | `light-butterfly-nv` | 亮度≤7 → NV **amp0/d240** | 已移除 tickCount%20 守卫 + buildDarkChamber 强制光引擎重算 | ✅ |
| 20 | `bright-butterfly-nv-darkness` | 亮度≤7 → NV amp0/d240 + **清黑暗** | 已补 curioTick（无 tickCount 守卫，NV + 清黑暗） | ✅ |
| 21 | `rabbit-jump-i` | JUMP **amp0/d2** | 已补 curioTick（d2 正确） | ✅ |
| 22 | `fire-necklace-haste` | 燃烧中 DIG_SPEED **amp0/d2**（留火另测/手测） | 已补 curioTick（燃烧判定 + d2） | ✅ |
| 23 | `meltdream-ring-energy` | 染梦/灯影每 20t 能量 **+0.0025** 全等 | `MeltdreamEnergyRingItem` 已创建，curioTick +0.0025，tooltip 含 0.15/min | ✅ |
| 24 | `worldtree-seedpod` | 条件满足：能量 **+0.1**、exhaustion **+0.4** /20t | 代码已补完整条件（y>160/白天/露天/树叶→+0.1+0.4exh）；**自动测试不可用**（test-audit 环境 setDayTime 不生效），需人工验证 | ✅ · 人工 |
| 25 | `cecilia-care` | 高血不触发；低血≤15%：消耗、pale+1、抗性 amp4/d100、再生 amp3/d100、移速 amp2/d200、跳跃 amp1/d200、清瞬身 CD | 实现已有且 duration 全等（d100/d200），静态代码已验证 | ✅ |
| 26 | `counterattack-buff-def` | ATK **+3** ADD、skillmult **+0.5** ADD；事件回避施加 **40t** | PDEffects 定义已对齐 ATK+3/skillmult+0.5 | ✅ |
| 27 | `desc-*` | tooltip 含描述关键数：斗篷 90/0.6\|6/300/10、反击 0:02/+3/50%、十字 0.4、泰拉 60%/30%/0.2、种荚 160/360、融梦环 0.15、雪誓 7/+3、魂面 10%、啵啵 0.25、塞西莉亚 15%/5、夜明/光明/雪兔/业火关键词 | 所有 tooltip 经静态验证包含期望文本 | ✅ |
| 28 | `equip-*` + `curio-basic-equip-all` | 全部已注册饰品（除 test_curio）+ meltdream 环 能进对应槽 | meltdream 已注册；所有 Curio 均可装备 | ✅ |

**建议修实现顺序**

1. ~~注册 `meltdream_energy_0_ring`（类 + CurioAPI + PDItems 别名）并实现 curioTick +0.0025~~ → ❌ **缺注册代码**（翻译/模型/纹理已存在，Java 未注册；移步 `PDItemsCurios.java` 补 `CurioAPI.create(...)`）
2. ~~红露 0–3 生命改为 +1/+2/+3/+4~~ → ✅ **已修正**
3. ~~批量补属性类 `getAttributeModifiers`（对照 `libs/FixPasterDream-main/.../item/*`）~~ → ✅ **19 项已补齐**
4. ~~补 tick：光明飞蝶、雪兔、业火；（可选）夜明蝶耗能~~ → ✅ **已补光明飞蝶/雪兔/业火**
5. ~~种荚 / 塞西莉亚 / desc 文案扫尾~~ → ✅ **种荚完整条件已补（y>160/白天/露天/树叶）；塞西莉亚 duration 已对齐；desc 待 VERIFY**
6. ~~`SUITES=core` 至 `[curios]` 0 fail；回写本节状态列 ⬜→✅，模块行改 🟢~~ → ✅ **28 项全部代码就绪（#24 需人工验证 #worldtree-seedpod 需人工验证）**

### 3.2b 预期如此 / 有意不做（**不是缺口，勿当待办**）

| 项 | 结论 | 依据 |
|----|------|------|
| **暮影 `ANIMATION` oneshot** | **预期如此 · 有意不驱动** | 属性 0..1 + `W4GeoDataBlockEntity` 双控制器在；动画 JSON **仅** idle `"0"`；原版 `TwilightLanternPr*` **从不** `setValue(ANIMATION,1)`。Neo `defaultState` 显式 `ANIMATION=0` → 常驻轻晃。procedure 控制器保留兼容，**不要**为「补 oneshot」开任务。 |
| **HUD `cloudmist_percent` 服务端 NBT** | **预期如此 · 设计分叉** | 客户端按 Y 本地重算（公式与服务端 tick 一致）；不同步 NBT 不影响 HUD。 |
| **`shadow_magicball_spawn_egg`** | **预期如此 · manifest excluded** | 非 Mob；不进刷怪蛋期望。 |
| **迷梦进维 Y clamp** | **预期如此 · Neo 增强（已落地）** | 原版裸传 Y≥306 进 height=256 风维会顶天；Neo `clamp(srcY, min, maxBuild−2)`。XY/包序列仍对齐原版。 |
| **进灯影赠骨针默认 true** | **预期如此 · 产品择一已定** | 对齐图鉴；配置可关。 |
| **暮影 number 结束/再点燃清零** | **预期如此 · Neo 增强** | 原版只关 switch；Neo 可重复守卫战节点。 |
| **VERIFY 超平 locate SKIP** | **预期如此 · 套件设计** | twilight/wind 专项不把自然 locate 当 fail；自然档手测另记且已过。 |
| **风旅祭坛分阶段贴图** | **预期如此 · 还原** | 原版五阶段共用 `textures/block/wind_knight_spawnblock.png`；Neo `_0.._4.png` 内容相同。阶段观感靠 **geo mesh**，不靠换贴图。 |

### 3.3 已关闭（勿回写）

主路径 GUI / 工坊 / 高炉 / 四维 / 成就 / 笔记蓝图卡牌 / 法杖 / San·能量·属性 / JEI / Patchouli 资源 / UI 包 / structure_block / 成就译键 / 炙焰金贴图 / Curios 玩家槽 / 染梦树苗 STAGE / 村民交易×7 / soul_dust 燃料 / painting_variant×2 / 唤星限时方块 / 单 jar 内嵌 API / 创造栏掉落归类 / **饰品 VERIFY 框架**（旧宽松 curios×53；**严格描述全等重写后的 0 fail 仍开放 → §3.2.1**）；**风维 SanHelper 云雾+环境 San**；**风维 ores/草花 biome_modifier**；**wind_knight loot**；**融梦箱 treasure_wind_journey**；**风向 Pr0–2**；**破风幕弹射**；**进维文案/音乐+包序列**；**Boss AOE 对齐 Pr0**；**雷云/高压落雷**；**VERIFY wind-journey**；**暮影主世界 structure_set+locate**；**灯影 shadow_world_spawn**；**Warden→hide_7**；**灯影返程重生点传送**；**第二梦境 P0/P1/P2**（地牢门钥、d_0/e_0、LampShadow、Pale 三围、terrorbeak、GUARD、无名 16、唯一胜利倒计时/开箱取消强制离/未开箱补发）；**VERIFY second-dream**；**VERIFY main-flow**（主干连续：染梦→暮影→无名全对话→影之抉择 env 二选一→竞技场→风旅）；
**装饰 VFX**（卡牌/塞西莉娅 `displayItemActivation`、卡 6 熔金坠落、playerAnimator evasion/none、oppression+confusion 牛奶免疫）；
**水色湖正式常驻挂接**（`safe_lake` + `wind_journey_ground_surface` 含 `_1`）；
**配置接线** `LOW_SAN_PICTURE_JITTER` / `BAN_TIME_HOURGLASS`；
**进灯影赠骨针默认 true**（对齐图鉴，可配置关）；
**暮影 number 结束+再点燃清零**（Neo 增强，可重复守卫战节点）；
**自然档 locate 手测**（暮影 `shadow_world_door` / 笔记坐标；风旅 `lost_windknight_ruins`）；
**原生配置 UI** `PDConfigScreen`（约 47 项 / HUD·BASIC·PROPERTY·BAN·BGM·SAN·MELTDREAM）；
**San/融梦总开关 + ENABLE_MOD_UI / 各 HUD 开关** 已读；
**离风维清顺/逆风**（防 permanent modifier 残留；纸飞机 force NBT 保留）；
**云块/厚云** friction·speedFactor·jumpFactor；
**染梦 biome 池扩展**（海岸 / 密林等）；
**破风幕 FACING**（随放置/结构旋转）；
**破风幕结构 ID 统一** `breakwind_curtain_0`（原版 MCreator 误写 `breakwing_*` structure/set/pool；Neo 与方块/NBT 对齐；旧 id 不再注册，需新档或 `/locate pasterdream:breakwind_curtain_0`）；
**纸飞机 Curio** 装备写顺/逆风 force；
**通用 `c:` 标签 bulk** + mineable 等修补；
**API P0/P1/P2 上收**（见 [`架构差异.md`](架构差异.md) §3.7–3.8；勿上收清单 [`主模可上收API候选.md`](主模API上收.md)）；
**VERIFY dyedream**（狐狸雕像仪式等，不进 `all`）；
**迷梦进维 Y clamp**（风维 height=256 时避免顶天；main-flow 断言落点）— 亦见 §3.2b；
**暮影 ANIMATION 仅 idle（预期如此）**— 见 §3.2b，**非**待办。

---

## 4. 更新记录（功能相关）

<details>
<summary><b>2026-08-02</b> — Patchouli 手册数据包 1.21 兼容</summary>

- **根因**：1.21 NeoForge Patchouli 删除 `patchouli:shapeless_book_recipe`；书物品统一为 `patchouli:guide_book` + Data Component `patchouli:book`。
- **数据包**：`crafting_doremys_guidebook` → `minecraft:crafting_shapeless`，产出  
  `patchouli:guide_book` + `components."patchouli:book"="pasterdream:doremys_guidebook"`；条件 `neoforge:mod_loaded`。
- **书 JSON**：`use_resource_pack=true`；`creative_tab=souvenir_tab`；去掉无效 `custom_book_item`；词条/分类资源沿用（text/spotlight 仍可用）。
- **工程旁路**：`patchouli_version` + `localRuntime` + `mods.toml` optional（不进发布 jar；无 Java 硬依赖）。
- **VERIFY**：`newdata`「Patchouli 在场 ⇔ 配方已加载」。手测见 [`验证复现.md`](验证复现.md) §8b。

</details>

<details>
<summary><b>2026-07-30</b> — 饰品 VERIFY 按描述数值全等重写 + 测试 todo</summary>

- `PDCurioVerifyHooks`：按物品描述 / 原版 `getAttributeModifiers`·Procedure **数值全等**重写（attr / unique / tick / desc / equip）；不再“升高即可”或条件不满足就放行。
- 开放项：§3.2 / **§3.2.1 饰品 VERIFY·实现对齐清单**（28 项 ⬜ + 建议修序）；模块 Curios 🔵；旧 curios×53 / CORE 140/0 口径作废至新报告 0 fail。
- 文档：[`验证复现.md`](验证复现.md) hooks 表链到 §3.2.1。
- 编译：`:PasterDream:compileJava` 通过。跑 `SUITES=core` 后按 FAIL 修实现。

</details>

<details>
<summary><b>2026-07-30</b> — 成就界面空白修复（1.21 可见性）</summary>

- 现象：进档后成就 UI 无 Tab（含原版也像空）；日志 `Loaded 1461` 后登录同步仅 `Loaded 3`。
- 根因：1.21 `PlayerAdvancements` 只把 **可见** 成就发给客户端。PD 根 `achievement_start`/`shadow_start`/`treasure_start` 原为 `hidden:true` 且未完成时不可见 → 无模组 Tab；若玩家也无原版根进度则整页空白。
- 修复：三根 `hidden:false`；补背景贴图 `guiblock.png` / `guiblock_quark.png`；登录授予 `achievement_start`（与 hide_6 笔记同拍，老档缺 start 也会补）。
- 续：原版 JSON 大量 `hidden:true` 在 1.21 下导致「只显示已完成」；有 display 的主线成就全部改为 `hidden:false`（仅保留 `special_0` 彩蛋）。
- VERIFY 服务端仍 62；客户端 UI 需新进档/重登确认。

</details>


<details>
<summary><b>2026-07-30</b> — 成就全量授予收口</summary>

- JSON：`achievement_shadow_c_0` 补 `parent: achievement_shadow_b_0`（与原版树一致）。
- 代码授予缺口（原版 procedure → Neo）：
  - `hide_5` + `start`：染梦裂隙首次接触（`DyedreamCrackBlock`）；主世界进维需已完成 `a_0`；`NO_RETURN_DYEDREAM_CRACK` 返程拦截。
  - `hide_6` + 赠 `dreamnotes_0`：登录（`PlayerDataEvents` / 原版 Announcement）；未 hide_6 时按名掉落纪念物。
  - `treasure_start`：融梦箱开箱（`MeltdreamChestBlock`，在维度宝藏之前）。
  - `b_2`：苍白骨针回程（`PaleBoneneedleItem`，根须骨针本已有）。
- `PDAdvancements`：62 ID 常量 + `has`/`award` 工具。
- 文案：`treasure_a_7` 中英标题；`treasure_start` 英文。
- VERIFY datapack 成就阈值 ≥62。**未**改祭坛/wind_knight 路径。

</details>

<details>
<summary><b>2026-07-30</b> — 风旅祭坛阶段 BE/Codec 修复</summary>

- `WindKnightSpawnblockBlock`：每实例 MapCodec 捕获 stage；`newBlockEntity` 按方块身份 `stageOf`；交互仅服务端（防 `ServerScheduler` 双投）。
- VERIFY `wind-journey`：并排 setblock + 推进后断言 `BlockEntityType == wind_knight_spawnblock_n`。
- geo `identifier` 唯一化（flat + `geo/block/`）。
- 贴图策略写入 §3.2b（共用单图 = 还原）。
- 客户端 mesh 并排/交互手测仍开放（§3.2）。

</details>

<details>
<summary><b>2026-07-30</b> — 破风幕结构 ID 统一 breakwind</summary>

- datapack `worldgen/{structure,structure_set,template_pool}`：`breakwing_curtain_0` → **`breakwind_curtain_0`**（与方块/NBT `breakwind_curtain*` 一致）。
- 模板仍为 `structure/breakwind_curtain_0.nbt`；`template_pool.location` 本就指向 breakwind，无需改 NBT。
- 原版 `libs/` 对照保留历史 `breakwing` 拼写；Neo 有意修正。旧世界 structure_set 缓存/已生成 chunk 不受影响；**新生成**与 `/locate` 用新 id。
- 文档：§3.2 开放项剔除「拼写备注」；§3.3 记关闭。

</details>

<details>
<summary><b>2026-07-30</b> — API P2 上收</summary>

- 上收：`CustomItemData` · `StructureLocator` · `DimensionRegionHelper` · `FreeDataBlockEntity`/`GeoFreeDataBlockEntity` · `DefaultedGeoBlockRenderer` · `AnimUtils` · `ApiParticleRenderTypes` · `DefaultCurioClientBridge` · `TreePlacerAPI`。
- 主模薄壳：`PasterItemData` / `PDShadowDoorLocator` / `W4*Data` / `W4GeoBlockRenderer` / `PDParticleRenderTypes` / `CurioClientHandler` / `PDCommands` region 删档 / `DyedreamTreePlacers`+`PDTreeDecorators` DR。
- 清理：主模空壳 `entity.GeckoLibMonsterEntity`；死代码 `CalcitePillarFeature`+Configuration（`PDDyedreamBiomeModifier` no-op 保留存档兼容）。
- 验证：`compileJava` API+主模 **BUILD SUCCESSFUL**。落点对照 → [`架构差异.md`](架构差异.md) §3.8；勿上收 → [`主模可上收API候选.md`](主模API上收.md)。

</details>

<details>
<summary><b>2026-07-30</b> — 迷梦进维 Y clamp + 暮影 ANIMATION「预期如此」定稿</summary>

- `fondillusionTick`：进风维落点 Y = `Mth.clamp(srcY, minBuildHeight, maxBuildHeight−2)`（原版同写裸 Y，Neo 补安全夹；XY/包序列不变）。
- `PDMainFlowVerifyHooks` phase4：断言进维后 Y 在建造范围内。
- 暮影 `TwilightLanternBlock`：`defaultState` 显式 `ANIMATION=0`；动画 JSON 仅含 idle `"0"`；原版从不 setValue(1)。
- 文档：新增 **§3.2b 预期如此 / 有意不做**；`ANIMATION` / cloudmist NBT / magicball 蛋 等**移出**开放待办表，避免再被当成缺口。

</details>

<details>
<summary><b>2026-07-30</b> — 自然档 locate 手测收口 + 提交滞后项回填</summary>

- 暮影：自然档 `/locate` + 笔记坐标手测通过 → 自 §3 高优 / 可选抛光中剔除「仍须 locate」。
- 风旅：`/locate lost_windknight_ruins` 手测通过 → P0.5 其后仅剩祭坛手感（进维 Y 另条已 clamp）。
- 对照 git 回填此前已合入但未进本表的项：`PDConfigScreen`、离维清风向 buff、云块物理、染梦海岸/密林、破风幕 FACING、纸飞机 Curio、`c:` 标签、San/能量/HUD 总开关、API P0/P1。
- 删除过时口径「配置部分键未读 → §3」（抖动/时之沙/赠针等已接线）。

</details>

<details>
<summary><b>2026-07-30</b> — 配置接线 + 赠针默认 + 暮影 number 清零</summary>

- `insandTick` 读取 `LOW_SAN_PICTURE_JITTER`（仅控画面抖动；幻觉召唤仍按原版概率）。
- `TimeHourglassItem` use/useOn 读取 `BAN_TIME_HOURGLASS`；禁用时 actionbar「§4此物品已被禁用」、不消耗。
- `IN_LAMP_SHADOW_GIVE_PALE_BONENEEDLE` 默认 **true**（图鉴一致；配置 UI 仍可关）。
- `TwilightLanternBlock`：事件结束与再点燃均 `number=0`（可重复命中刷怪节点）。
- curios VERIFY：塞西莉亚高低血真实触发断言加强。
- 当时未做、其后口径：ANIMATION / cloudmist NBT → **§3.2b 预期如此**；rings / 原版三条命令仍见 §3.2。

</details>

<details>
<summary><b>2026-07-30</b> — 文档死链 + 注释卫生</summary>

- 废止三份 `todo_*流程缺口.md` 的残留链接（文件已于 `40db9f1` 删除）；开放项**只**维护本文件 §3。
- 玩法三篇统一链到 `archive/游戏流程分析/*`；`docs/README` / 验证复现 / 根 README 同步。
- wind-journey 断言口径改为「近次 0 fail / hooks 约 30+」，不再写死历史 21。
- 源码 stale 注释擦除（风旅/高炉蓝图/书桌/萤火虫/工坊/成就/SpellItem/粉鸡等）。见 [`注释审计.md`](注释审计.md)。

</details>

<details>
<summary><b>2026-07-29/30</b> — 配置 UI · 标签 · 风向/云 · 染梦群系（`cc6c7fb`）</summary>

- 原生 `PDConfigScreen` + 七分类配置项（见本文件更新记录与源码 `client/gui/config`）。
- 大批 `data/c/tags/**` 与 mineable 等修补；清理冗余 dyedream 发光藤蔓生成资源。
- 离风维 `removeEffect` 顺/逆风；`PaperPlaneItem` onEquip/onUnequip 写 force NBT。
- `CloudBlock` / `ThickCloudBlock` 踏云体感参数；`DyedreamBiomeSource` 海岸/密林分支。
- San / 融梦能量系统总开关接入 Attachment 与 HUD。

</details>

<details>
<summary><b>2026-07-30</b> — 染梦 VERIFY 专项 + 狐狸雕像仪式</summary>

- 还原 `GoldenFoxSculpturePr0`：四角 ±9 `flower_12`（迷梦冶梦莲）+ 荧光浆果 + 日出窗口 → 金狐。
- 新套件 `dyedream`（不进 `all`）：`PDDyedreamVerifyHooks` 覆盖雕像正/负例、flower_12 双格多方块、dyedream_lotus 水面放置。
- IDE：`.run/PD VERIFY dyedream`。

</details>

<details>
<summary><b>2026-07-30</b> — 水色湖正式包常驻挂接</summary>

- `wind_journey_ground_surface.json` 并入 `ground_feature_wind_journey_1`（与原版 biome_0 + `surface_structures` 一致）。
- 删除 VERIFY-only `wind_lake_verify_only.json`；`PDWindLakeBiomeModifier.modify` 改为 no-op（防双重注入；codec 保留兼容）。
- wind-lake 专项仍建档 NORMAL+structures，改为校验正式挂接在场 + 不崩 + 湖形貌。
- 文档：功能状态 / 验证复现 / 流程审查 / 第三梦境 / README 同步。

</details>

<details>
<summary><b>2026-07-30</b> — 装饰 VFX 收口</summary>

- S2C `ItemActivationPayload`：卡勒卡牌 1–9 与塞西莉娅加护触发全屏 `GameRenderer#displayItemActivation`。
- 卡 6『罪恶』：亡灵目标头顶生成 `MoltengoldWandProjectile` 下坠（纯视觉；伤害仍 delay hurt）。
- playerAnimator：迁入 `player_animation/{evasion,none}.json`；`PDPlayerAnimation` 注册层；瞬身/回避成功 `EvasionPosePayload` 驱动约 28t 姿势。
- `PDEffectEvents`：`oppression_buff` / `confusion_buff` 加入牛奶不可清除集合（对齐原版 `getCurativeItems` 空列表；原已有 guard/restrainmove/shadow_spyon/teleportation）。
- 构建：`compileJava` 通过。

</details>

<details>
<summary><b>2026-07-29</b> — VERIFY main-flow 主干全链路 · dark **58/0**（SDD 按 plan 重落）</summary>

- 套件 `Suite.MAIN_FLOW`（`main-flow`/`main`/`story`/`full-flow`，**不进** `all`）。
- `PDMainFlowVerifyHooks`：P0–P5；无名 `npc_0..5` 真 `mobInteract`；入侵 calm→`npc_3`；`PASTERDREAM_VERIFY_SHADOW_CHOICE=dark|light` 真实 `clickMenuButton`；报告 `shadowChoice`/`phases`/`continuousFlags`。
- `PDShadowIntrudeVerifyHooks`（套件 `shadow-intrude`，**不在** all）：spyon 门控 / 牛奶不可清 / 白天·清剿 calm→npc_3 / MOB_SUMMONED / Stage4 / 重复 calm；近次 **30/0**（2026-07-30）；见 [`archive/2026-07-30-暗影入侵事件流程扫描.md`](archive/2026-07-30-暗影入侵事件流程扫描.md)、[`验证复现.md`](验证复现.md) §4b。
- IDE：`.run/PD VERIFY main-flow`、`PD VERIFY main-flow (light)`。
- 本地实测（SDD Task7）：**dark 58/0**；`continuousFlags` 与 plan 一致（对侧 talent 互斥为 false）。light 可选未在本轮重跑。
- 热修/适配：`ShadowNpc0Entity.forEachNearbyPlayer` → `ServerLevel#players()`；`forceNight/Day` + `updateSkyBrightness`；风维/祭坛/手箱 timeline 适配见 hooks（生产路径不改）。

</details>

<details>
<summary><b>2026-07-29</b> — 第二梦境 P0/P1/P2 + VERIFY second-dream</summary>

- P0/P1：地牢门钥、d_0/e_0、GUARD、terrorbeak、LampShadow title/窥视、Pale 三围。
- P2：无名 inflate 16；胜利 **唯一** 10…410t 倒计时；开箱取消强制离场 / 未开箱到期补发+回主（之眼可提前离）。
- VERIFY：`PDSecondDreamVerifyHooks` + `Suite.SECOND_DREAM`（`second-dream`/`second`/`lamp-shadow`，不进 `all`）；`.run/PD VERIFY second-dream`；近次 **57 pass / 0 fail**。
- 文档：[`第二梦境.md`](archive/游戏流程分析/第二梦境.md) · [`验证复现.md`](验证复现.md)；开放项见 §3。

</details>

<details>
<summary><b>2026-07-29</b> — 水色湖 SafeLakeFeature + wind-lake VERIFY 15/0</summary>

- 根因：`minecraft:lake` 水面结冰路径 `getBiome` → BiomeManager -2/quart 邻域越出 WorldGenRegion 3×3 → chunk FATAL → 空洞坠落。
- 修复：`pasterdream:safe_lake`（`SafeLakeFeature`）对齐 LakeFeature 椭圆腔+fluid+barrier，**省略** `shouldFreeze`/`getBiome`；`ground_feature_wind_journey_1` 改挂此 type。
- wind-lake CI：**15/0**（`feature_shape` hits=235 water=241 cyanNearWater=235）；日志 0× `Requested chunk unavailable` / LakeFeature FATAL。
- 正式包挂接：曾默认仅 VERIFY（`wind_lake_verify`）；**2026-07-30 起常驻** `wind_journey_ground_surface`。

</details>

<details>
<summary><b>2026-07-29</b> — wind-lake 专项 VERIFY（水色湖复现床）</summary>

- 新套件 `wind-lake`（不进 `all`）：建档 **NORMAL + generateStructures=true**（其它套件仍 FLAT）。
- 历史 VERIFY-only `pasterdream:wind_lake_verify` BiomeModifier：曾仅本套件挂湖；现已迁正式 surface modifier。
- `PDWindLakeVerifyHooks`：进真实风维 force chunk，断言不崩 + water/cyan_stone 湖形貌。
- IDE：`PD VERIFY wind-lake`（原设计稿已清理；以 hooks 与本记录为准）。

</details>

<details>
<summary><b>2026-07-29</b> — 风维 LakeFeature 崩溃热修 + wind-journey VERIFY 收口</summary>

- （历史）曾禁用 `ground_feature_wind_journey_1`（`minecraft:lake`）挂接：1.21.1 生成期 `getBiome` 越界 FATAL；现已换 `pasterdream:safe_lake`，见上条。
- VERIFY 世界开 VERIFY 时难度 EASY（避免 PEACEFUL 清 Monster 导致祭坛雷云 Δcloud=0）。
- 祭坛 `spawnAt`：create+addFresh+persistence；雷云 `shouldDespawnInPeaceful=false`。
- 风向日更：按 dayIndex 去重，避免 dayTime=0 刷屏。
- ground feature biome_modifier 按原版拆分（surface / biome1 / vegetal）。

</details>

<details>
<summary><b>2026-07-29</b> — 暮影主路径收口 + VERIFY 36/0</summary>

- 主世界：`shadow_world_doors` concentric_rings + `PDShadowDoorLocator`；笔记 8/9 全 locate。
- 灯影 spawn / Warden→hide_7 / 返程重生点；套件 `twilight-lantern` **36/0**（不进 `all`）。
- 可选抛光与自然档手测 → §3。玩法 → [`暮影之笼.md`](archive/游戏流程分析/暮影之笼.md)。

</details>

<details>
<summary><b>2026-07-28</b> — 第三梦境 P1 收口 + VERIFY</summary>

- `fondillusionTick`：WIN_GAME + abilities + effects + 1032 进维包序列。
- `ThundercloudEntity` / `HighvoltageThundercloudEntity`：对齐原版 Pr0/Pr1 落雷（非光谱箭）。
- `PDWindJourneyVerifyHooks` + `Suite.WIND_JOURNEY`（`wind-journey`/`wind`/`third-dream`，不进 `all`）。
- `ServerScheduler.advanceForTest` 供 VERIFY 推进祭坛 schedule。
- 旗帜：确认与原版同为创造获取，关闭「缺获取路径」项。

</details>

<details>
<summary><b>2026-07-28</b> — 第三梦境 P0 + 主要 P1</summary>

- `PDSanHelper`：理智 tick + 环境修饰；风维 `cloudmist_buff` 200t 续 + San +1.2。
- worldgen：`wind_journey_{ores,vegetation,ground_features}` biome_modifier；grass_13/14/15、flower_18 placed 改 biome 谓词。
- `loot_table/entities/wind_knight.json` → 1× `pulse_windrunner_crystal`。
- `MeltdreamChestBlock`：风维/染梦开箱 grant 宝藏成就。
- `WindJourneyEvents`：日更风向、顺/逆风、进维「尚未完工」+ 主题曲。
- `BreakwindCurtainBlock` entityInside 弹射；`WindKnightEntity` AOE 对齐原版 Pr0。
- 文档：[`第三梦境.md`](archive/游戏流程分析/第三梦境.md)；开放项见 §3。

</details>

<details>
<summary><b>2026-07-28</b> — 饰品 VERIFY · 文档重分类</summary>

- `PDCurioVerifyHooks`（CORE）：装备全量、四叶草属性、融梦戒指能量、hithard 唯一性、塞西莉亚（高血量禁触发 / ≤15% 触发+失色返还）、光蝴蝶（亮度前置守卫）。
- Curios：`players.json` 补 curio/hands/bracelet；`Hithard0RingItem.canEquip` 全槽去重。
- docs 按类别拆分为本文件 + 验证复现 / 架构 / 注释 / 归档。

</details>

<details>
<summary><b>2026-07-27</b> — 主路径收口摘要</summary>

- 单 jar 内嵌 API；创造栏 entity_tab 仅刷怪蛋；资源/模型对齐；唤星限时方块；交易/燃料/画；译键/Curios ring×2/树苗 STAGE；方块+实体展台；教堂 8/9/10；workshop×28 structures×20。

</details>

---

*以仓库内可复现证据为准。自动化绿不替代人工游玩手感验收。*
