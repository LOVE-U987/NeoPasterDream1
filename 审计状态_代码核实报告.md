# 审计状态核实报告

对代码审计报告的42个问题逐项核验后的结果，以及新发现的18个Bug核实结果

---

## P0 - 严重问题

| # | 问题 | 文件位置 | 核实结果 | 修复状态 |
|---|------|----------|----------|----------|
| #1 | **并发修改异常风险**<br>Iterator 遍历 `ArrayList`，任务回调修改列表导致 `ConcurrentModificationException` | `AaroncosLefthand0Entity.java:356-365` | 已确认 | 未计划 |
| #2 | **双重检查锁定失效**<br>`musicManager` 未声明为 `volatile`，可能暴露部分构造的对象 | `PDClientEvents.java:86` | 已确认 | 未计划 |
| #3 | **交叉淡入淡出状态泄漏**<br>字段在空值检查前设置，若查找失败会误停其他音乐 | `CrossfadeManager.java:78-83` | 已确认 | 未计划 |
| #4 | **NaN速度导致崩溃**<br>`target.position().subtract(entity.position()).normalize()` 当两者位置重合时产生 `Vec3(NaN,NaN,NaN)`，传入 `setDeltaMovement()` 可导致实体卡死甚至崩溃 | `EntitySkillManager.java:260-261` | 已确认 | 未计划 |

## P1 - 高优先级

| # | 问题 | 文件位置 | 核实结果 | 修复状态 |
|---|------|----------|----------|----------|
| #5 | **投射物双倍速度**<br>`tick()` 调用 `super.tick()` 后又手动 `setPos()`，每 tick 移动两次 | `SquealWaveProjectileEntity.java:46-50` | 已确认 | 未计划 |
| #6 | **声音重复播放**<br>服务端 `playSound()` + 客户端 `playLocalSound()`，玩家听到两次 | `BoneWingFireBallProjectileEntity.java:78-88` | 已确认 | 未计划 |
| #7 | **NBT 状态未持久化**<br>`useActive`/`useTicks`/`activatingPlayerUUID` 未保存到 NBT | `LifeCrystalBlockEntity.java:55-58` | 已确认 | 未计划 |
| #8 | **玩家登出时水晶被销毁**<br>`useTicks >= 40` 时无条件设空气，玩家下线后水晶消失 | `LifeCrystalBlockEntity.java:217-237` | 已确认 | 未计划 |
| #9 | **冷却时空音乐名称**<br>`musicName` 为 null 仍传入冷却管理器，结束时停止所有音乐 | `ModMusicManager.java:211-218` | 已确认 | 未计划 |
| #10 | **音乐突然切断**<br>`stopAllMusic()` 直接 `stop()`，无淡出效果 | `ModMusicManager.java:265-271` | 已确认（已有 TODO） |  |
| #11 | **假交叉淡入淡出**<br>仅计数后停止旧音乐，新旧轨道全音量同时播放 | `CrossfadeManager.java:101-115` | 已确认（已有 TODO） |  |
| #12 | **GUI 立即打开**<br>`scheduleTick()` 调度但 `getTicker()` 返回空 lambda，GUI 提前打开 | `ShadowChestBlock.java:143-160` | 已确认 | 未计划 |
| #13 | **`canContinueToUse()` 错误委托**<br>自定义 RangedAttackGoal 的 `canContinueToUse()` 调用了 `canUse()`，导致每tick重新评估攻击起点而非继续条件，冷却周期异常重启 | `ShadowSquealGhostEntity.java:91-95`<br>`ShadowSquealGhost0Entity.java:141-144` | 已确认 | 未计划 |
| #14 | **吼叫冷却永不自然衰减**<br>`roarCooldown` 仅在 `hurt()` 内递减，未在 `baseTick()` 处理。若实体200+tick不受伤，冷却永远不会过期 | `TerrorbeakEntity.java:126` | 已确认 | 未计划 |
| #15 | **`isPlaying()` 永久为true**<br>非循环BGM播放完毕后 `currentSound` 从未被置null，导致 `isPlaying()` 永远返回 `true`，阻碍重启逻辑 | `MusicPlaybackController.java:84-86` | 已确认 | 未计划 |
| #16 | **强制类型转换风险**<br>`(SimpleParticleType) PDParticles.MELTDREAM_CRYSTAL_PARTICLE.particleType()` 无 `instanceof` 保护，类型不匹配时抛出 `ClassCastException` | `LifeCrystalBlockEntity.java:205` | 已确认 | 未计划 |
| #17 | **跨生物群系时音乐切换滞后3秒**<br>交叉淡入淡出期间 `tick()` 提前返回，完全跳过生物群系变化检测，玩家穿越群系边界时音乐切换延迟整个淡出周期 | `ModMusicManager.java:199-201` | 已确认 | 未计划 |

## P2 - 中等优先级

| # | 问题 | 文件位置 | 核实结果 | 修复状态 |
|---|------|----------|----------|----------|
| #18 | **目标优先级重复**<br>`RandomStrollGoal` 和 `MeleeAttackGoal` 同为优先级 3，竞争 | `ShadowSquealGhostEntity.java:143,154` | 已确认 | 未计划 |
| #19 | **目标优先级重复**<br>`MeleeAttackGoal` 和 `RangedAttackGoal` 同为优先级 1 | `ShadowSquealGhost0Entity.java:137,140` | 已确认 | 未计划 |
| #20 | **远程攻击空实现**<br>`performRangedAttack()` 方法体为空，只有 TODO | `ShadowSquealGhost0Entity.java:322-326` | 已确认 | 未计划 |
| #21 | **NBT 持久化缺失**<br>`size` 字段未保存，重载后重置为 0 | `SmallStoneSpiritEntity.java:58,155-162` | 已确认 | 未计划 |
| #22 | **数值无限增长**<br>`spirit.size += 0.1` 无上限检查，长期游戏可能溢出 | `SmallStoneSpiritEntity.java:200` | 已确认 | 未计划 |
| #23 | **吼叫触发逻辑错误**<br>咆哮触发在免疫伤害检查之前，火焰/仙人掌/凋灵也会触发 | `TerrorbeakEntity.java:100-133` | 已确认 | 未计划 |
| #24 | **空闲动画未设置**<br>`idlePredicate` 始终返回 `CONTINUE`，空闲动画未激活 | `DreamCauldronBlockEntity.java:73-75` | 已确认 | 未计划 |
| #25 | **物品生成位置问题**<br>固定 `Y+0.8` 生成，未检测上方阻挡 | `MeltdreamChestBlockEntity.java:261-268` | 已确认 | 未计划 |
| #26 | **双重 tick 调度**<br>`super.onPlace()` 已调度，又重复 `scheduleTick()` | `MeltdreamLiquidBlock.java:48-71` | 已确认 | 未计划 |
| #27 | **客户端/服务端 tick 不必要**<br>客户端 `getTicker()` 调用 `tick()` 但内部跳过所有逻辑 | `ShadowVortexBlock.java:66-74` | 已确认 | 未计划 |
| #28 | **状态未持久化**<br>`age` 字段未保存到 NBT，重载后从 0 重新计数 | `ShadowVortexBlockEntity.java:57` | 已确认 | 未计划 |
| #29 | **击退覆盖速度**<br>`setDeltaMovement(knockback)` 完全替换目标现有速度 | `EntitySkillManager.java:260-263` | 已确认 | 未计划 |
| #30 | **冷却管理器空值检查缺失**<br>`pendingBiomeId` 为 null 时调用 `equals()` 会抛 NPE | `CooldownManager.java:74` | 已确认 | 未计划 |
| #31 | **播放状态不精确**<br>`isPlaying()` 未调用 `SoundManager.isActive()` 确认 | `MusicPlaybackController.java:84-86` | 已确认 | 未计划 |
| #32 | **冗余 `instanceof` 检查**<br>`entity instanceof Mob` 后再判 `instanceof LivingEntity` 为恒真式（Mob extends LivingEntity），死代码 | `FoxFireEntity.java:165-166` | 已确认 | 未计划 |
| #33 | **`setSwitchCooldownTicks()` 为空操作**<br>方法体只打日志，不实际修改冷却时间，调用方会静默失败 | `ModMusicManager.java:162` | 已确认 | 未计划 |
| #34 | **静默物品丢失**<br>水晶槽生成时若 `level.getServer()` 为null，槽位仍被清空但物品未生成 | `MeltdreamChestBlockEntity.java:256-266` | 已确认 | 未计划 |
| #35 | **每秒tick做 `instanceof` 检查**<br>`getTicker()` 返回lambda内每tick做运行时类型判断，应用 `createTickerHelper` 模式替代 | `ShadowVortexBlock.java:66-74` | 已确认 | 未计划 |
| #36 | **着火时间过长**<br>火球设置 `setRemainingFireTicks(2000)` = 100秒着火，远超合理范围 | `BoneWingFireBallProjectileEntity.java:139` | 已确认 | 未计划 |
| #37 | **文档与实际不符**<br>`ANIMATION=0` 被注释为"闲置循环动画"实为"未激活/可交互"状态 | `MeltdreamChestBlock.java:47-48` | 已确认 | 未计划 |
| #38 | **羽毛粒子尺寸无限增长**<br>`quadSize *= 1.001f` 无上限，与 `CrackParticle` 同类问题 | `FeatherWhiteParticle.java:87` | 已确认 | 未计划 |
| #39 | **银色粒子尺寸无文档化收缩**<br>`quadSize *= 0.999f` 缓慢缩小，行为未在Javadoc中说明 | `SilverParticle.java:67` | 已确认 | 未计划 |

## P3 - 低优先级

| # | 问题 | 文件位置 | 核实结果 | 修复状态 |
|---|------|----------|----------|----------|
| #40 | **类型使用不当**<br>`skillTime` 为 `double` 但仅与 `int` 比较和运算 | `ShadowGolemEntity.java:65` | 已确认 | 未计划 |
| #41 | **自我效果应用**<br>自身是 `Mob`，会对自己施加 `MOVEMENT_SLOWDOWN` | `FoxFireEntity.java:164-168` | 已确认 | 未计划 |
| #42 | **魔法数字未文档化**<br>`skillTime < 189` 阈值含义不明确 | `ShadowGolemEntity.java:105` | 已确认 | 未计划 |
| #43 | **客户端/服务端状态不一致**<br>客户端 `mobInteract()` 始终返回 `SUCCESS` | `ShadowNpc0Entity.java:139-148` | 已确认 | 未计划 |
| #44 | **文档与代码不一致**<br>注释 70/22/8% vs 实际 50/30/20% | `MeltdreamChestBlock.java:47-48` | 已确认 | 未计划 |
| #45 | **空 ticker**<br>多个方块注册了空 ticker lambda | 多处 | 已确认 | 未计划 |
| #46 | **冗余空值检查**<br>注册表值不可能为 null | `MeltdreamLiquidBlock.java:65` | 已确认 | 未计划 |
| #47 | **粒子淡出缺失**<br>仅实现淡入，lifetime 结束突然 `remove()` | `SilverParticle.java:56` | 已确认 | 未计划 |
| #48 | **粒子淡出缺失**<br>`alpha` 渐入后保持 1.0，突然消失 | `LeavesParticle.java:81-96` | 已确认 | 未计划 |
| #49 | **速度未阻尼**<br>`yd` 每 tick 增加 0.00016，持续累积 | `FeatherWhiteParticle.java:76` | 已确认 | 未计划 |
| #50 | **粒子无限增长**<br>`quadSize *= 1.002f` 无上限 | `CrackParticle.java:66` | 已确认 | 未计划 |
| #51 | **占位符按钮**<br>`handleCraftButton()`/`handleClearButton()` 只打日志 | `DreamCauldronScreen.java:123-135` | 已确认 | 未计划 |
| #52 | **常量重复定义**<br>`BIOME_DYEDREAM_*` 在多处重复定义 | 多处 | 已确认 | 未计划 |
| #53 | **GL 状态恢复依赖外部**<br>`GLOWING_SHEET.begin()` 修改 GL 状态但未实现 `end()` | `PDParticleRenderTypes.java:28-46` | 已确认 | 未计划 |
| #54 | **冗余调用**<br>`Minecraft.getInstance()` 重复调用 | `ModMusicManager.java:249` | 已确认 | 未计划 |
| #55 | **服务端空 ticker**<br>服务端分支无实际逻辑 | `DreamAccumulatorBlock.java:109-117` | 已确认 | 未计划 |
| #56 | **早期初始化风险**<br>Mixin 在客户端未完全初始化时调用注册表 | `MinecraftMixin.java:46` | 已确认 | 未计划 |
| #57 | **每次技能触发重复创建数组**<br>`new int[]{57,70,83,88,95,105,112}` 应提取为 `static final` 常量 | `AaroncosLefthand0Entity.java:411` | 已确认 | 未计划 |
| #58 | **双端空ticker浪费CPU**<br>`DreamAccumulatorBlock` 在客户端和服务端都注册了空lambda ticker，完全无逻辑执行 | `DreamAccumulatorBlock.java:109-117` | 已确认 | 未计划 |
| #59 | **每tick创建临时对象**<br>mixin中 `getSituationalMusic` 每tick调用完整注册表链，应缓存 `isCustomDimension` 结果 | `MinecraftMixin.java:46` | 已确认 | 未计划 |
| #60 | **影子NPC动画延迟**<br>客户端 `mobInteract` 在检查 `hasInteracted` 前返回，动画仅通过服务端网络同步，有可见延迟 | `ShadowNpc0Entity.java:139-148` | 已确认 | 未计划 |

---

## 统计摘要

| P级 | 报告数 | 已确认数 | 占比 |
|-----|--------|----------|------|
| P0 | 4 | 4 | 6.7% |
| P1 | 13 | 13 | 21.7% |
| P2 | 22 | 22 | 36.7% |
| P3 | 21 | 21 | 35% |
| **总计** | **60** | **60** | **100%** |

**已知问题**：**#10**（无淡出停止音乐）和 **#11**（假交叉淡入淡出）在代码中已有 TODO 标注，为已标记未实现项。

# 修复注意事项
为避免多人同时修复一个问题，在修复前请提前申领占用