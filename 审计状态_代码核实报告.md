# 审计状态核实报告

对代码审计报告的42个问题逐项核验后的结果，以及新发现的18个Bug核实结果。

> **结案（2026-07-26）**：60 项全部处理完毕（56 项修复 + 4 项核实无需改：#1 重构已消除、#5 审计误报、#36 与原版一致、#53 符合 vanilla 惯例）。  
> **关联终验（2026-07-27）**：主路径移植客户端双模验证 **130/130 PASS**（见 `功能还原差距报告.md` 第一节、提交 `5f465da`）。本表状态不再回退。

---

## P0 - 严重问题

| # | 问题 | 文件位置 | 核实结果 | 修复状态 |
|---|------|----------|----------|----------|
| #1 | **并发修改异常风险**<br>Iterator 遍历 `ArrayList`，任务回调修改列表导致 `ConcurrentModificationException` | `AaroncosLefthand0Entity.java:356-365` | 已确认 | ✅ 核实无需修复：重构(a0eb6df)已移至AaroncosHandEntity，遍历模式安全 |
| #2 | **双重检查锁定失效**<br>`musicManager` 未声明为 `volatile`，可能暴露部分构造的对象 | `PDClientEvents.java:86` | 已确认 | ✅ 已修复：volatile+synchronized，完整构造后发布 |
| #3 | **交叉淡入淡出状态泄漏**<br>字段在空值检查前设置，若查找失败会误停其他音乐 | `CrossfadeManager.java:78-83` | 已确认 | ✅ 已修复：先校验查找结果再提交状态 |
| #4 | **NaN速度导致崩溃**<br>`target.position().subtract(entity.position()).normalize()` 当两者位置重合时产生 `Vec3(NaN,NaN,NaN)`，传入 `setDeltaMovement()` 可导致实体卡死甚至崩溃 | `EntitySkillManager.java:260-261` | 已确认 | ✅ 已修复：lengthSqr<1e-8 防护，重合时取零向量 |

## P1 - 高优先级

| # | 问题 | 文件位置 | 核实结果 | 修复状态 |
|---|------|----------|----------|----------|
| #5 | **投射物双倍速度**<br>`tick()` 调用 `super.tick()` 后又手动 `setPos()`，每 tick 移动两次 | `SquealWaveProjectileEntity.java:46-50` | 已确认 | ✅ 核实非缺陷：直接继承Projectile（super.tick()无位移），手动setPos是唯一移动来源，已补注释 |
| #6 | **声音重复播放**<br>服务端 `playSound()` + 客户端 `playLocalSound()`，玩家听到两次 | `BoneWingFireBallProjectileEntity.java:78-88` | 已确认 | ✅ 已修复：仅保留服务端playSound |
| #7 | **NBT 状态未持久化**<br>`useActive`/`useTicks`/`activatingPlayerUUID` 未保存到 NBT | `LifeCrystalBlockEntity.java:55-58` | 已确认 | ✅ 已修复：saveAdditional/loadAdditional持久化三字段 |
| #8 | **玩家登出时水晶被销毁**<br>`useTicks >= 40` 时无条件设空气，玩家下线后水晶消失 | `LifeCrystalBlockEntity.java:217-237` | 已确认 | ✅ 已修复：玩家离线仅重置状态，保留水晶方块 |
| #9 | **冷却时空音乐名称**<br>`musicName` 为 null 仍传入冷却管理器，结束时停止所有音乐 | `ModMusicManager.java:211-218` | 已确认 | ✅ 已修复：musicName为null不进冷却/不停音乐 |
| #10 | **音乐突然切断**<br>`stopAllMusic()` 直接 `stop()`，无淡出效果 | `ModMusicManager.java:265-271` | 已确认（已有 TODO） | ✅ 已修复：beginFadeOutAll逐tick渐弱后自停 |
| #11 | **假交叉淡入淡出**<br>仅计数后停止旧音乐，新旧轨道全音量同时播放 | `CrossfadeManager.java:101-115` | 已确认（已有 TODO） | ✅ 已修复：VolumeSoundInstance(AbstractTickableSoundInstance)真音量交叉渐变，60tick |
| #12 | **GUI 立即打开**<br>`scheduleTick()` 调度但 `getTicker()` 返回空 lambda，GUI 提前打开 | `ShadowChestBlock.java:143-160` | 已确认 | ✅ 已修复：按原版实现延迟16tick真实调度开GUI（ShadowChestBlockEntity.serverTick） |
| #13 | **`canContinueToUse()` 错误委托**<br>自定义 RangedAttackGoal 的 `canContinueToUse()` 调用了 `canUse()`，导致每tick重新评估攻击起点而非继续条件，冷却周期异常重启 | `ShadowSquealGhostEntity.java:91-95`<br>`ShadowSquealGhost0Entity.java:141-144` | 已确认 | ✅ 已修复：删除两处错误覆盖，恢复vanilla继续条件 |
| #14 | **吼叫冷却永不自然衰减**<br>`roarCooldown` 仅在 `hurt()` 内递减，未在 `baseTick()` 处理。若实体200+tick不受伤，冷却永远不会过期 | `TerrorbeakEntity.java:126` | 已确认 | ✅ 已修复：递减移入baseTick每tick处理 |
| #15 | **`isPlaying()` 永久为true**<br>非循环BGM播放完毕后 `currentSound` 从未被置null，导致 `isPlaying()` 永远返回 `true`，阻碍重启逻辑 | `MusicPlaybackController.java:84-86` | 已确认 | ✅ 已修复：isPlaying改用SoundManager.isActive判定 |
| #16 | **强制类型转换风险**<br>`(SimpleParticleType) PDParticles.MELTDREAM_CRYSTAL_PARTICLE.particleType()` 无 `instanceof` 保护，类型不匹配时抛出 `ClassCastException` | `LifeCrystalBlockEntity.java:205` | 已确认 | ✅ 已修复：改用holder()类型安全引用，无强转 |
| #17 | **跨生物群系时音乐切换滞后3秒**<br>交叉淡入淡出期间 `tick()` 提前返回，完全跳过生物群系变化检测，玩家穿越群系边界时音乐切换延迟整个淡出周期 | `ModMusicManager.java:199-201` | 已确认 | ✅ 已修复：淡出期间持续检测群系，redirectCrossfade重定向（含折返反转） |

## P2 - 中等优先级

| # | 问题 | 文件位置 | 核实结果 | 修复状态 |
|---|------|----------|----------|----------|
| #18 | **目标优先级重复**<br>`RandomStrollGoal` 和 `MeleeAttackGoal` 同为优先级 3，竞争 | `ShadowSquealGhostEntity.java:143,154` | 已确认 | ✅ 已修复：近战3/闲逛4/张望5错开 |
| #19 | **目标优先级重复**<br>`MeleeAttackGoal` 和 `RangedAttackGoal` 同为优先级 1 | `ShadowSquealGhost0Entity.java:137,140` | 已确认 | ✅ 已修复：远程1/近战3/闲逛4/张望5 |
| #20 | **远程攻击空实现**<br>`performRangedAttack()` 方法体为空，只有 TODO | `ShadowSquealGhost0Entity.java:322-326` | 已确认 | ✅ 已修复：按原版实现为发射SquealWave声波弹 |
| #21 | **NBT 持久化缺失**<br>`size` 字段未保存，重载后重置为 0 | `SmallStoneSpiritEntity.java:58,155-162` | 已确认 | ✅ 已修复：SpiritSize持久化到NBT |
| #22 | **数值无限增长**<br>`spirit.size += 0.1` 无上限检查，长期游戏可能溢出 | `SmallStoneSpiritEntity.java:200` | 已确认 | ✅ 已修复：上限25.6（HEALTH_BOOST 255级封顶对应值） |
| #23 | **吼叫触发逻辑错误**<br>咆哮触发在免疫伤害检查之前，火焰/仙人掌/凋灵也会触发 | `TerrorbeakEntity.java:100-133` | 已确认 | ✅ 已修复：免疫判定前置，免疫伤害不再触发咆哮 |
| #24 | **空闲动画未设置**<br>`idlePredicate` 始终返回 `CONTINUE`，空闲动画未激活 | `DreamCauldronBlockEntity.java:73-75` | 已确认 | ✅ 已修复：setAndContinue空闲动画"0"；坩埚还原中追加"craft"触发动画 |
| #25 | **物品生成位置问题**<br>固定 `Y+0.8` 生成，未检测上方阻挡 | `MeltdreamChestBlockEntity.java:261-268` | 已确认 | ✅ 已修复：上方阻挡检测，被挡改从箱子正面弹出 |
| #26 | **双重 tick 调度**<br>`super.onPlace()` 已调度，又重复 `scheduleTick()` | `MeltdreamLiquidBlock.java:48-71` | 已确认 | ✅ 已修复：hasScheduledTick判重（审计所述super调度为流体tick，与方块tick独立，已注释澄清） |
| #27 | **客户端/服务端 tick 不必要**<br>客户端 `getTicker()` 调用 `tick()` 但内部跳过所有逻辑 | `ShadowVortexBlock.java:66-74` | 已确认 | ✅ 已修复：createTickerHelper模式，仅注册服务端ticker |
| #28 | **状态未持久化**<br>`age` 字段未保存到 NBT，重载后从 0 重新计数 | `ShadowVortexBlockEntity.java:57` | 已确认 | ✅ 已修复：age持久化到NBT |
| #29 | **击退覆盖速度**<br>`setDeltaMovement(knockback)` 完全替换目标现有速度 | `EntitySkillManager.java:260-263` | 已确认 | ✅ 已修复：击退叠加到现有速度，垂直取max |
| #30 | **冷却管理器空值检查缺失**<br>`pendingBiomeId` 为 null 时调用 `equals()` 会抛 NPE | `CooldownManager.java:74` | 已确认 | ✅ 已修复：补null防御分支（核实原equals方向实际不抛NPE，防御性补齐） |
| #31 | **播放状态不精确**<br>`isPlaying()` 未调用 `SoundManager.isActive()` 确认 | `MusicPlaybackController.java:84-86` | 已确认 | ✅ 已修复：与#15同处修复 |
| #32 | **冗余 `instanceof` 检查**<br>`entity instanceof Mob` 后再判 `instanceof LivingEntity` 为恒真式（Mob extends LivingEntity），死代码 | `FoxFireEntity.java:165-166` | 已确认 | ✅ 已修复：删除恒真instanceof嵌套 |
| #33 | **`setSwitchCooldownTicks()` 为空操作**<br>方法体只打日志，不实际修改冷却时间，调用方会静默失败 | `ModMusicManager.java:162` | 已确认 | ✅ 已修复：真正透传到CooldownManager（≥1钳制） |
| #34 | **静默物品丢失**<br>水晶槽生成时若 `level.getServer()` 为null，槽位仍被清空但物品未生成 | `MeltdreamChestBlockEntity.java:256-266` | 已确认 | ✅ 已修复：先生成物品实体再清空槽位 |
| #35 | **每秒tick做 `instanceof` 检查**<br>`getTicker()` 返回lambda内每tick做运行时类型判断，应用 `createTickerHelper` 模式替代 | `ShadowVortexBlock.java:66-74` | 已确认 | ✅ 已修复：同#27 |
| #36 | **着火时间过长**<br>火球设置 `setRemainingFireTicks(2000)` = 100秒着火，远超合理范围 | `BoneWingFireBallProjectileEntity.java:139` | 已确认 | ✅ 核实与原版一致：原版即setSecondsOnFire(100)=100秒，有意设计，已补注释标明依据 |
| #37 | **文档与实际不符**<br>`ANIMATION=0` 被注释为"闲置循环动画"实为"未激活/可交互"状态 | `MeltdreamChestBlock.java:47-48` | 已确认 | ✅ 已修复：注释更正为"未激活/可交互"状态 |
| #38 | **羽毛粒子尺寸无限增长**<br>`quadSize *= 1.001f` 无上限，与 `CrackParticle` 同类问题 | `FeatherWhiteParticle.java:87` | 已确认 | ✅ 已修复：上限=初始尺寸×1.5 |
| #39 | **银色粒子尺寸无文档化收缩**<br>`quadSize *= 0.999f` 缓慢缩小，行为未在Javadoc中说明 | `SilverParticle.java:67` | 已确认 | ✅ 已修复：Javadoc补充缩小行为说明（有意设计） |

## P3 - 低优先级

| # | 问题 | 文件位置 | 核实结果 | 修复状态 |
|---|------|----------|----------|----------|
| #40 | **类型使用不当**<br>`skillTime` 为 `double` 但仅与 `int` 比较和运算 | `ShadowGolemEntity.java:65` | 已确认 | ✅ 已修复：改为int，NBT兼容旧double |
| #41 | **自我效果应用**<br>自身是 `Mob`，会对自己施加 `MOVEMENT_SLOWDOWN` | `FoxFireEntity.java:164-168` | 已确认 | ✅ 已修复：谓词排除自身 |
| #42 | **魔法数字未文档化**<br>`skillTime < 189` 阈值含义不明确 | `ShadowGolemEntity.java:105` | 已确认 | ✅ 已修复：提取SKILL_CHARGE_THRESHOLD/HURT_CHARGE_BONUS/HURT_CHARGE_CAP常量+注释 |
| #43 | **客户端/服务端状态不一致**<br>客户端 `mobInteract()` 始终返回 `SUCCESS` | `ShadowNpc0Entity.java:139-148` | 已确认 | ✅ 已修复：新增DATA_HAS_INTERACTED同步字段，双端sidedSuccess语义一致 |
| #44 | **文档与代码不一致**<br>注释 70/22/8% vs 实际 50/30/20% | `MeltdreamChestBlock.java:47-48` | 已确认 | ✅ 已修复：注释更正为实际50/30/20% |
| #45 | **空 ticker**<br>多个方块注册了空 ticker lambda | 多处 | 已确认 | ✅ 已修复：清理DreamAccumulator(双端)/TheEndlessBookOfDreamSeekers(服务端)/MeltdreamChest(客户端)；ShadowChest改为真实ticker |
| #46 | **冗余空值检查**<br>注册表值不可能为 null | `MeltdreamLiquidBlock.java:65` | 已确认 | ✅ 已修复：删除冗余判空，holder()去强转 |
| #47 | **粒子淡出缺失**<br>仅实现淡入，lifetime 结束突然 `remove()` | `SilverParticle.java:56` | 已确认 | ✅ 已修复：尾段40tick线性淡出 |
| #48 | **粒子淡出缺失**<br>`alpha` 渐入后保持 1.0，突然消失 | `LeavesParticle.java:81-96` | 已确认 | ✅ 已修复：尾段20%淡出+落地alpha防回跳 |
| #49 | **速度未阻尼**<br>`yd` 每 tick 增加 0.00016，持续累积 | `FeatherWhiteParticle.java:76` | 已确认 | ✅ 已修复：上浮速度封顶0.03格/tick |
| #50 | **粒子无限增长**<br>`quadSize *= 1.002f` 无上限 | `CrackParticle.java:66` | 已确认 | ✅ 已修复：上限=初始尺寸×1.5 |
| #51 | **占位符按钮**<br>`handleCraftButton()`/`handleClearButton()` 只打日志 | `DreamCauldronScreen.java:123-135` | 已确认 | ✅ 已修复：完整还原原版坩埚——7槽菜单+196×196屏幕+合成按钮(78,71)+2000mB融梦液罐+桶注入+5配方+t58/59/60时序+动画音效粒子；5种法术物品已注册且可施放（法术战斗模块已完整还原） |
| #52 | **常量重复定义**<br>`BIOME_DYEDREAM_*` 在多处重复定义 | 多处 | 已确认 | ✅ 已修复：新建registry/PDBiomes常量类，4处重复全部收敛 |
| #53 | **GL 状态恢复依赖外部**<br>`GLOWING_SHEET.begin()` 修改 GL 状态但未实现 `end()` | `PDParticleRenderTypes.java:28-46` | 已确认 | ✅ 核实符合vanilla惯例：ParticleEngine.render末尾统一恢复状态，无实际污染路径，已补注释 |
| #54 | **冗余调用**<br>`Minecraft.getInstance()` 重复调用 | `ModMusicManager.java:249` | 已确认 | ✅ 已修复：复用tick()顶部mc局部变量 |
| #55 | **服务端空 ticker**<br>服务端分支无实际逻辑 | `DreamAccumulatorBlock.java:109-117` | 已确认 | ✅ 已修复：getTicker整体删除 |
| #56 | **早期初始化风险**<br>Mixin 在客户端未完全初始化时调用注册表 | `MinecraftMixin.java:46` | 已确认 | ✅ 已修复：level==null防护提前返回 |
| #57 | **每次技能触发重复创建数组**<br>`new int[]{57,70,83,88,95,105,112}` 应提取为 `static final` 常量 | `AaroncosLefthand0Entity.java:411` | 已确认 | ✅ 已修复：提取SWORD_RAIN_HIT_TICKS静态常量 |
| #58 | **双端空ticker浪费CPU**<br>`DreamAccumulatorBlock` 在客户端和服务端都注册了空lambda ticker，完全无逻辑执行 | `DreamAccumulatorBlock.java:109-117` | 已确认 | ✅ 已修复：同#55 |
| #59 | **每tick创建临时对象**<br>mixin中 `getSituationalMusic` 每tick调用完整注册表链，应缓存 `isCustomDimension` 结果 | `MinecraftMixin.java:46` | 已确认 | ✅ 已修复：@Unique字段按维度ResourceKey缓存 |
| #60 | **影子NPC动画延迟**<br>客户端 `mobInteract` 在检查 `hasInteracted` 前返回，动画仅通过服务端网络同步，有可见延迟 | `ShadowNpc0Entity.java:139-148` | 已确认 | ✅ 已修复：与#43合并，客户端同读同步状态即时播放动画 |

---

## 统计摘要

| P级 | 报告数 | 已确认数 | 占比 |
|-----|--------|----------|------|
| P0 | 4 | 4 | 6.7% |
| P1 | 13 | 13 | 21.7% |
| P2 | 22 | 22 | 36.7% |
| P3 | 21 | 21 | 35% |
| **总计** | **60** | **60** | **100%** |

**已知问题**：**#10**（无淡出停止音乐）和 **#11**（假交叉淡入淡出）在代码中已有 TODO 标注，为已标记未实现项。（2026-07-26 已随本轮修复实现）

---

# 修复执行记录（2026-07-26）

本轮对全部 60 项进行了处理并通过 `:PasterDreamAPI:compileJava` + `:PasterDream:compileJava` 编译验证：

| 结果 | 数量 | 编号 |
|------|------|------|
| 已修复 | 56 | 其余全部 |
| 核实无需修改 | 4 | #1（重构时已消除）、#5（审计误报：直接继承Projectile无位移逻辑）、#36（与原版数值一致）、#53（符合vanilla状态管理惯例） |

**主要新增/重构**：
- 音乐系统：`VolumeSoundInstance`（可逐tick调音量的BGM载体）+ `FadeState` 状态机（IDLE/CROSSFADE/FADING_OUT），实现真·交叉淡入淡出、带淡出的停止、淡出期间群系变化重定向；新建 `registry/PDBiomes.java` 收敛群系Key常量。
- 梦境炼药锅（#51）：发现原3+1槽布局与原版GUI纹理（实为196×196）不对齐，已按原版完整还原为7槽+2000mB融梦液罐+5法术配方+炼制时序状态机（t=0/20/40音效粒子、t=58扣料、t=59出品、t=60弹出），合成按钮走vanilla clickMenuButton通道；同步还原5种法术物品（闪电/剧毒/治疗/狂暴/冰冻，含原版纹理模型与中英文案）与dust_0粒子。法术施法已于同日随后完整还原：5 个法术投射物实体 + 狂暴/治疗立场实体 + 冰冻/狂暴属性效果 + 6 个法术粒子 + 4 个法术音效，命中效果时序（落雷/毒雾三波/冻结五波/立场治疗）与原版 procedure 一致。
- 暗影箱（#12）：按原版procedure还原16tick延迟开箱（BlockEntity ticker调度）。

## 游戏实测与二轮修复（2026-07-26 晚）

自动化冒烟测试（runClient + 模组内 PDSmokeTest，47 项断言全部 PASS）验证：零崩溃启动、炼药锅全链路实机可用（注液 1000mb→合成→GUI 关闭→液体 900mb→材料消耗→闪电法术弹出）、3 项暗影成就正常加载。实测暴露的问题已全部修复：

| 类别 | 修复内容 |
|------|---------|
| 构建阻塞 | `.trae/tools/check_lang.py` 两处缺陷：① BlockItem 误要求 item.* 键（运行时实际走 block.* 键）② `registerBlock()` 模式缺失导致 Phase2 方块漏扫 |
| Phase2 方块 | `cyan_stone_pillar` blockstate（facing→axis，匹配 RotatedPillarBlock）、`breakwind_curtain` blockstate（去 facing，无属性方块） |
| 配方 ×20 | 15 个切石配方 1.20 旧格式（result 字符串→对象）；3 个融梦工具引用改名物品（meltdream_crystal→_0）；shadow_bed/shadow_hand_lantern 产物未注册→加 neoforge:item_exists 加载条件（物品移植后自动生效） |
| 战利品表 ×14 | 5 个 id→name 字段修正；scute→turtle_scute（1.21 改名）；8 张宝箱表中 7 种未移植物品条目→minecraft:empty 占位（保留权重，待内容移植后恢复）；孤儿表 cyan_stone_smooth 删除 |
| 潜在隐患 | 69 个 JSON 的 UTF-8 BOM 清除（6 数据包 + 63 资源） |
| 纹理引用 | thick_shadow→thick_shadow_block（2 模型）；dream_traveler_belt 模型指向实际纹理；补 shadow_shelf_3.png（原版拼音纹理改名）与 air.png（影门系列引用） |
| 其它 | 补 shadow_vortex 双语方块名；移除孤悬的 music.demo_music 音效键；新增 PDSmokeTest 自动冒烟测试类（PASTERDREAM_SMOKETEST=1 时激活，日常零影响） |

已知遗留（非缺陷）：5 首规划中的群系 BGM 音频未制作（音乐 API 优雅跳过）、29 条字幕翻译、2 个玩偶模型 GeckoLib 几何版本警告、run/saves 下遗留 test-audit 与 New World 两个测试存档可手动删除。

## 三轮修复（2026-07-26 夜，移植期新发现缺陷）

| 缺陷 | 定性 | 修复 |
|------|------|------|
| `server.tell(new TickTask(tick, task))` 当定时器使用（15 处：法术时序/竞技场/闪避拖尾/NPC对话/卡牌/骨针等） | 产品级——vanilla 该队列在服务器空闲时立即执行，所有延迟塌缩为同 tick 爆发（三轮客户端取证） | 新建 `util/ServerScheduler`（PriorityQueue + ServerTickEvent.Post 真定时），15 处全量替换 |
| 验证框架时间线序列器 ArrayDeque 按插入序出队 | 框架级——非单调书写导致队头堵塞、断言批量同 tick 连发，制造"治疗立场零回血"假阴性（治疗产品逻辑经证清白） | 换按 dueTick 排序的 PriorityQueue；治疗查询顺手改为 vanilla 区域实体惯用形态 |

四轮实测最终 33 pass / 6 fail（6 个均为待移植内容的覆盖缺口），行为级断言全绿。

# 修复注意事项
为避免多人同时修复一个问题，在修复前请提前申领占用